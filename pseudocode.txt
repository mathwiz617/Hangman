import java.util.ArrayList 
import java.util.scanner
import java.util.Random
import java.io.*

Main Class:

	Scanner keyboard = new Scanner(System.in)
	Word word = new Word()
	String go = "yes"
	String input = ""
	int strikes = 0
	char[] goal
	
	do{
		print(choose a difficulty: easy, medium, or hard: )
	 	input = keyboard.nextLine()
		
		if(input = easy){
			word.load("easy.txt");
			word.chooseWord();	
			goal = word.generateGoal();
			System.out.print(goal);
			System.out.println();
		}
		if(input = medium){
			word.load("medium.txt");
			word.chooseWord();	
			goal = word.generateGoal();
			System.out.print(goal);
			System.out.println();
		}
		if(input = hard){
			word.load("hard.txt");
			word.chooseWord();	
			goal = word.generateGoal();
			System.out.print(goal);
			System.out.println();
		}
		
		load(file)
		
		chooseWord()
		
		goal = generateGoal()
		
		print(Play again? Yes to play again: )
		
		
	}while(go = "yes")
	
Word Class:

	private	ArrayList<String> wordList <- like an array, but resizable
	private string word
	private	char[] goal
	
	public Word(){
		wordList = new ArrayList<>() <- empty ArrayList, to be filled with strings
		word = ""
	}
	
	public char[] getGoal(){
		return goal
	}
	
	public void setGoal(char[] letters){
		goal = letters
		return
	}
	
	public String getWord(){
		return goal
	}
	
	public void setWord(String string){
		word = string
		return
	}
	
	public ArrayList<String> getGoal(){
		return wordList
	}
	
	public void setGoal(ArrayList<String> list){
		wordList = list
		return
	}
	
	public void load(String file){
		File loadFrom = new File(file)
		Scanner inputFile = new Scanner(file)
		
		while(inputFile.hasNext()){
			String nextWord = inputFile.nextLine()
			wordList.add(nextWord.trim())
		}
		inputFile.close()
		return
	}
	
	public void chooseWord(){
		int size = wordList.size()
		int index = random.nextInt(size)
		word = wordList.get(index)
		return
	}
	
	public generateGoal(){
		goal = word.toCharArray();
	}
	
Guess Class(String(char?)guess):
	for (int i = 0; i < word.length(); i++) {
		if (guess == i) {
			print(i)
		}
		else {
			print(_)
			wrong++
		}
	if (wrong = 1) { determine how many wrong = hangman and finish loop 
	print(display of 1 wrong)
	
	Guess Class (if someone enters "guess" they can guess a word and see if right):
	print(What is your guess?)
	wordGuess = scan.nextLine();
	if (wordGuess.equals(word)) {
		*win sequence*
		}
	else {
		wrong++
		}
		
	
	